[pytest]
testpaths = tests
markers =
    unit: no I/O, no network — always runnable
    integration: filesystem I/O via tmp_path, no network
    api: REST contract tests with mock server
    live: requires a running server or real hardware — excluded from CI
